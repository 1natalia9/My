В Git единицей хранения данных является объект (англ. object), который однозначно определяется 40-символьным
 хешем sha1. В объектах Git хранит почти всё: коммиты, содержимое файлов, их иерархию. Сначала объекты 
представляют из себя обычные файлы в папке .git/objects, а после git gc упаковываются в .pack-файлы.
 Для экономии дискового пространства содержимое всех объектов дополнительно сжимается с помощью zlib.
Также хранение объектов целиком позволяет делать надежное слияние веток с разрешением конфликтов.

Какой командой нужно воспользоватся чтобы узнать свой user.neme и user.email в git bash?

 git config user.name
 git config user.email

Какой последовательностью команд нужно воспользоватся чтобы создать локальный репозиторий и добавить
 в него файлы?

  cd C:/Users/user/my_project
  git init newprojekt
  ls
  echo "New fail" > new.txt

Какая последовательность команда добавит новый файл index коммита?

ls -a
git add new.txt

Какая команда загрузит новый репозиторий c github.com? что предварительно нужно сделать на сервере
 github.com.

создать репозиторий
git clone shhkey

 
Как создать новый репозиторий на github.com из локальной консоли git bash?

create toking

curl -u 'USER:TOKEN' https://api.github.com/user/repos -d '{"name":"test"}'

git remote add origin shhkey

